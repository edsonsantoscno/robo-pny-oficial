version: '3.8'
services:
  trader:
    build: .
    container_name: robo_mestre_24h
    restart: always
    environment:
      - TZ=America/Sao_Paulo
    # Se você for usar as variáveis do Portainer, elas entram aqui automaticamente
