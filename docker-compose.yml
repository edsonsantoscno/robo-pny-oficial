version: '3.8'
services:
  trader:
    image: python:3.11-slim
    container_name: robo_mestre_24h
    restart: always
    build: .
    environment:
      - TZ=America/Sao_Paulo
    # As variáveis de ambiente (API Keys) você preenche no painel do Portainer
